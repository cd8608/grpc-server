{
  "name": "grpc-server",
  "version": "1.0.0",
  "description": "location-server",
  "scripts": {
    "start-debug": "node --inspect --debug-brk grpc-server.js",
    "start": "node grpc-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "grpc": "^1.10.1",
    "avsc":"^5.2.3",
    "kafka-node":"^2.5.0"
  }
}